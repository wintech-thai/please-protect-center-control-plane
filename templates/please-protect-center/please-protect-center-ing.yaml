apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: please-protect-center-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.pleaseProtectCenterDomain }}
  rules:
    - host: {{ .Values.pleaseProtectCenterDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pp-{{ .Values.envGroup }}-please-protect-center
                port:
                  number: 80
